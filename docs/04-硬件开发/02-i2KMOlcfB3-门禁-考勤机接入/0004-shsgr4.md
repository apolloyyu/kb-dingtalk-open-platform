---
title: "二维码接入"
source_url: "https://open.dingtalk.com/document/development/shsgr4"
namespace: "development"
slug: "shsgr4"
group: "硬件开发"
tab: "门禁/考勤机接入"
breadcrumb: "二维码接入"
doc_id: "B8sDmpHsVM"
updated_at: "2026-08-04 09:07:12"
---

> Source: https://open.dingtalk.com/document/development/shsgr4
> Path: 硬件开发 / 门禁/考勤机接入 / 二维码接入
> Updated: 2026-08-04 09:07:12

# 二维码接入

## **动态二维码接入**

### **头文件**

```
#include "dtiot_netconfig_bind_service.h"
```

### **启动动态二维码绑定**

**场景：**启动动态二维码绑定

**函数：**

```
 int (*dynamic_bind_start)(void (*on_qrcode_update)(int length, char* qrcode));
```

需要传入一个函数指针用于接收动态二维码的更新，用于显示绑定。

### **停止动态二维码绑定**

**场景：**停止动态二维码绑定

**函数：**

```
int (*dynamic_bind_stop)(void);
```

## **静态二维码接入**

### **头文件**

```
#include "dtiot_netconfig_bind_service.h"
```

### **启动静态二维码绑定**

**场景：**启动静态二维码绑定

**函数：**

```
  int (*static_bind_start)();
```

### **停止静态二维码绑定**

**场景：**停止静态二维码绑定

**函数：**

```
  int (*static_bind_stop)(void);
```

## **摄像头反向二维码接入**

### **头文件**

```
#include "dtiot_netconfig_bind_service.h"
```

### **摄像头配网接入**

**场景：**摄像头扫描到二维码后，通过二维码获取需要连的wifi的信息。

**函数：**

```
int (*camera_bind_get_wifi_by_qrcode)(char qrcode[DEVICE_QR_CODE_LEN], dtiot_wifi_info_t *wifi_info);
```

**参数：**

```
//扫描的二维码
char qrcode[DEVICE_QR_CODE_LEN];

//调用方传入wifi 信息的指针
dtiot_wifi_info_t *wifi_info

详细请参考头文件。
```

### **设备配网成功**

**场景：**待wifi连接成功后，设置网络状态。

**函数:**

```
int (*set_net_status)(int net_status);
```
