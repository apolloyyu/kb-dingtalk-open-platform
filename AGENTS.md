# AGENTS.md — 知识库入口

你被"先读 AGENTS.md"带到这里——这就是唯一入口，读完即可工作，其余文件按需打开。

本库是钉钉开放平台文档中心 **2026-09-01 快照**（8 大类 / 27 子类 / 3624 篇；权威日期见 `meta/source_manifest.json`）+ 索引 + 图谱 + 查询 CLI，离线自足。用途：回答钉钉开放平台开发问题。

本库是知识库组的一员,专注**钉钉开放平台域**。dws CLI 的整理版知识在独立仓库 [kb-dws-wiki](https://github.com/apolloyyu/kb-dws-wiki);跨域问题(如 dws 报了平台 errcode)两库都查。


三个前提：**外挂目录**（`$WIKI` 指本目录绝对路径，命令均 cwd 无关）；答疑**只读**；答案只从库内来，没有就如实说，**不编造**。

## 怎么查

**主线：`dkdoc ctx` 一次打包 →（未覆盖所问才）分步精查 → 沿 `dkdoc links` / `graph/hubs.md` 扩展。**

```bash
python3 $WIKI/bin/dkdoc ctx <词>...        # ★首选：实体五表命中+top2正文(含source_url)+links相邻+检索审计行
python3 $WIKI/bin/dkdoc find <关键词>...   # 标题/面包屑/小标题/slug 模糊定位（多词=AND）
python3 $WIKI/bin/dkdoc api|err|event|jsapi|perm <词>   # 精确实体五表（未命中自动兜底）
python3 $WIKI/bin/dkdoc cat|links <路径|slug>     # 读正文 / 查正反链
python3 $WIKI/bin/dkdoc grep '<词>' [子路径]      # 全文兜底
```

| 问题类型 | 入口 |
|---|---|
| 高频主题（token/免登/机器人/事件/卡片…） | `index/TOPICS.md` 直达 |
| 精确实体（接口/错误码/事件名/JSAPI 名/权限点） | `dkdoc api / err / event / jsapi / perm` |
| 领域盘点、存在性问法（"有哪些/是否有 X"） | `index/INDEX.md` 逐级下钻，或 `dkdoc find` 后按"有哪些/没有哪些"作答 |
| 模糊描述 → 兜底 | `dkdoc find`（换几组词）→ `dkdoc grep` |

**检索优先于追问**：先查再答；确需澄清也要附上已查到的候选方向，禁止未检索就反问、只反问不作答。

**逐级放宽仍无**：换英文 slug 重试 → grep 报错原文/字段名 → 查 `meta/UNAVAILABLE.md`、`meta/failures.json`（抓取盲区）与 kb_manifest 的 tier（`◇边缘` 裁剪）→ 确认没有就声明"快照未覆盖"，给最接近的官方入口。

**域边界**：管理后台开关、产品功能配置、配额上限、价格、客户端产品行为——属帮助中心/产品域，本快照只收**开发者文档中心**。识别出这类问题直接声明边界并转介（管理后台对应设置 + 官方客服/帮助中心），不要硬找，也不要编造后台开关名。

## 怎么答（三条原则）

1. **有据**——一切断言以库内原文为据：实体名（接口/JSAPI/事件/字段/权限点）逐字取自文档；字段行为、数字规格（长度/限额/有效期/QPS）以原文为准，原文没写的写"文档未说明"；快照没有的实体不给推测名，要给必须标注（推测）。**否定结论有门槛**：说"不支持/没有/不行"之前，必须查完对应实体表 + `find` + `grep` 三轮；查不到只能说"快照未见，以线上文档为准"，不下绝对否定。
2. **裁决**——多篇冲突：`updated_at` 新者 → 现行版 → `api.dingtalk.com`。凡引用 `⚠归档` 内容（含其中的数字规格）必须标注归档并给现行替代。时效敏感问题（计费/灰度/上线时间）声明快照日期。
3. **交付**——引用只用文档 frontmatter 的 `source_url` 线上链接，本地路径/快照文件名/行号一律不出现在回答里；用户问题的每个子维度都要回应，库内没有的显式写"快照未见"，不静默略过；回答末尾固定「参考文档」一节。

发送前对照三条原则自检一遍。

---

维护不属于答疑任务。只有被**明确要求**更新/维护本库时，才去读 [ops/INGEST.md](ops/INGEST.md)。
